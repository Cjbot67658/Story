# StoryBot
Telegram Story Bot.

## Local dev (Termux)
1. Fill `.env` with your keys.
2. pip install -r requirements.txt
3. ./start.sh

## Deploy
- Use Koyeb / Railway / Heroku.
- Set environment variables on the platform (API_ID, API_HASH, BOT_TOKEN, MONGO_URI, OWNER_IDS, DB_CHANNEL_ID).
